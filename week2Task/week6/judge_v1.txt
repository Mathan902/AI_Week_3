You are grading ONE answer from the Acme SDK documentation assistant against ONE binary criterion.

CRITERION: DIRECTLY ANSWERS
PASS if a developer who asked this question could act on this answer alone: it states the specific fact, value, behaviour or usage they asked for, and that content is correct according to the documentation shown.
FAIL if any of these is true:
- it quotes related text from the docs that does not actually answer what was asked (for example a sentence next to the fact, instead of the fact);
- it answers a different question from the one asked;
- it says "I don't know" although the documentation shown contains the answer.
An "I don't know" is PASS only if the documentation shown really does not contain the answer.
Use the SDK version the question asks about. If it names no version, the developer is on SDK v3.

Do NOT judge any of the following. Code checks them separately, so ignore them completely:
code formatting or whether code parses, whether endpoint paths exist, whether the SDK version is labelled, deprecation or migration notes, repeated sentences, and citation format.

QUESTION:
{question}

DOCUMENTATION SHOWN TO THE ASSISTANT (top 3 retrieved chunks):
{context}

ANSWER:
{answer}

Reply with only this JSON object and nothing else:
{"verdict": "PASS" or "FAIL", "reason": "<one sentence>"}
